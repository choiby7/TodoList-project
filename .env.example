# ================================
# Database Configuration
# ================================
DB_USERNAME=todolist_user
DB_PASSWORD=your_strong_password_here

# ================================
# Redis Configuration
# ================================
REDIS_PASSWORD=your_redis_password_here

# ================================
# JWT Configuration
# ================================
# JWT Secret Key (최소 256bit / 32자 이상 권장)
# Spring Boot에서 jwt.secret으로 인식됨
JWT_SECRET=your_jwt_secret_key_minimum_32_characters_long_for_security

# JWT Access Token 유효기간 (밀리초, 기본: 1시간 = 3600000)
# Spring Boot에서 jwt.access-token-validity로 인식됨
JWT_ACCESS_EXPIRATION=3600000

# JWT Refresh Token 유효기간 (밀리초, 기본: 7일 = 604800000)
# Spring Boot에서 jwt.refresh-token-validity로 인식됨
JWT_REFRESH_EXPIRATION=604800000

# ================================
# Google OAuth2 Configuration
# ================================
# Google Cloud Console에서 발급받은 클라이언트 ID
GOOGLE_CLIENT_ID=your_google_client_id_here

# Google Cloud Console에서 발급받은 클라이언트 시크릿
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# ================================
# Application Configuration
# ================================
# Spring Boot 프로파일
SPRING_PROFILES_ACTIVE=prod

# 프론트엔드 URL (CORS 및 OAuth2 리다이렉트용)
FRONTEND_URL=http://localhost

# ================================
# Optional: Development Settings
# ================================
# 개발 환경에서만 사용
# SPRING_PROFILES_ACTIVE=local
# FRONTEND_URL=http://localhost:3000
